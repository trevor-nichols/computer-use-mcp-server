# native-input placeholder

This repository chose a **pure Swift** first pass for macOS input injection so the native bridge could stay in one executable during the first implementation phase.

This directory is reserved for a future Rust-based input port if we decide to split input injection out of the Swift helper later.
